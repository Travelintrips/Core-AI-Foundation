# WP-06A Furniture Placement Foundation

This project is a self-contained, dependency-free implementation of the WP-06A furniture placement foundation. It includes a deterministic placement engine, admin-only preview/apply endpoints, snapshot protection, and an interactive review dashboard.

## User preferences

- Prefer functional, deterministic behavior over mock-only UI.
- Keep the implementation free of LLM/provider dependencies.