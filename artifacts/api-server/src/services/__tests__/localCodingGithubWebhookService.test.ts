import{createHmac}from"crypto";import{describe,expect,it,vi}from"vitest";vi.mock("@workspace/db",()=>({db:{},aiCodingGithubDeliveriesTable:{}}));vi.mock("../aiEventBusService.js",()=>({publish:vi.fn()}));import{verifyGithubWebhookSignature}from"../localCodingGithubWebhookService.js";describe("github coding webhook",()=>{it("accepts a valid sha256 signature",()=>{const b=Buffer.from('{"ok":true}');const s="secret";const sig="sha256="+createHmac("sha256",s).update(b).digest("hex");expect(verifyGithubWebhookSignature(b,sig,s)).toBe(true)});it("rejects invalid and missing signatures",()=>{const b=Buffer.from("{}");expect(verifyGithubWebhookSignature(b,"sha256=00","secret")).toBe(false);expect(verifyGithubWebhookSignature(b,undefined,"secret")).toBe(false)})});
