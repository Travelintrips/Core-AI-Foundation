/**
 * _pdfWorkerImpl.ts — Phase 2 PDF Export Worker (internal helpers)
 *
 * This module is imported by jobWorkerService.ts and contains the full
 * implementation of executePdfExportJob().
 *
 * Separated to keep jobWorkerService readable.
 */

// NOTE: This file is intentionally not standalone — see jobWorkerService.ts
// which imports and calls executePdfExportJobImpl().

export {}; // module boundary only — see jobWorkerService.ts
