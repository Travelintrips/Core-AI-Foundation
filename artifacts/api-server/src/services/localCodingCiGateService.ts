import{and,eq}from"drizzle-orm";import{aiCodingCiBindingsTable,aiCodingWorkstreamsTable,db}from"@workspace/db";import{publishSafe}from"./aiEventBusService.js";
type CiPayload={repository?:unknown;headSha?:unknown;status?:unknown;conclusion?:unknown;pullRequestNumber?:unknown};
export async function handleCodingGithubCiEvent(event:{eventId:string;eventType:string;payloadJson:unknown}){const p=(event.payloadJson??{})as CiPayload;if(typeof p.repository!=="string"||typeof p.headSha!=="string")return{ok:true,skipped:"missing_binding_keys"};const rows=await db.select().from(aiCodingCiBindingsTable).where(and(eq(aiCodingCiBindingsTable.repository,p.repository),eq(aiCodingCiBindingsTable.headSha,p.headSha)));if(rows.length===0)return{ok:true,skipped:"unbound_sha"};for(const binding of rows){const [ws]=await db.select().from(aiCodingWorkstreamsTable).where(eq(aiCodingWorkstreamsTable.id,binding.workstreamId)).limit(1);if(!ws||ws.headSha!==binding.headSha){await db.update(aiCodingCiBindingsTable).set({state:"STALE",lastCheckpointJson:{eventId:event.eventId,reason:"workstream_head_sha_mismatch"}}).where(eq(aiCodingCiBindingsTable.id,binding.id));continue;}const completed=p.status==="completed";const success=completed&&p.conclusion==="success";const failure=completed&&typeof p.conclusion==="string"&&p.conclusion!=="success";const state=success?"GREEN":failure?"FAILED":"WAITING";await db.update(aiCodingCiBindingsTable).set({state,lastCheckpointJson:{eventId:event.eventId,eventType:event.eventType,status:p.status??null,conclusion:p.conclusion??null,pullRequestNumber:p.pullRequestNumber??null}}).where(eq(aiCodingCiBindingsTable.id,binding.id));publishSafe({eventType:success?"coding.ci.green":failure?"coding.ci.failed":"coding.ci.progress",sourceModule:"coding-ci-gate",sourceId:binding.id,correlationId:event.eventId,payload:{workstreamId:binding.workstreamId,repository:binding.repository,headSha:binding.headSha,pullRequestNumber:binding.pullRequestNumber,nextAction:success?"CONTINUE_WITH_EXISTING_APPROVAL_GATES":failure?"REVIEW_CI_FAILURE":"WAIT_FOR_REQUIRED_CHECKS"}});}return{ok:true,bindings:rows.length};}
