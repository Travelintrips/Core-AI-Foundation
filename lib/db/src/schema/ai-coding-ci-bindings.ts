import { appSchema } from "./_pg-schema";import{index,jsonb,text,timestamp,uniqueIndex,uuid}from"drizzle-orm/pg-core";import{aiCodingWorkstreamsTable}from"./ai-coding-task-graph";
export const aiCodingCiBindingsTable=appSchema.table("ai_coding_ci_bindings",{id:uuid("id").primaryKey().defaultRandom(),workstreamId:uuid("workstream_id").notNull().references(()=>aiCodingWorkstreamsTable.id,{onDelete:"cascade"}),repository:text("repository").notNull(),pullRequestNumber:text("pull_request_number").notNull(),headSha:text("head_sha").notNull(),requiredChecksJson:jsonb("required_checks_json").notNull().default([]),state:text("state").notNull().default("WAITING"),lastCheckpointJson:jsonb("last_checkpoint_json").notNull().default({}),createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp("updated_at",{withTimezone:true}).notNull().defaultNow().$onUpdate(()=>new Date())},t=>[uniqueIndex("ai_coding_ci_bindings_workstream_uidx").on(t.workstreamId),index("ai_coding_ci_bindings_repo_sha_idx").on(t.repository,t.headSha)]);
