# P0 File Impact Matrix

Companion to `p0-enterprise-foundation-implementation-spec.md`, `p0-work-package-plan.md`. Rows are derived from direct source-code inventory (file/line citations from the explorer passes), not documentation alone. "Work Package" references `p0-work-package-plan.md`. Risk: Low/Medium/High per spec's own risk callouts.

| Domain | File | Function/Component | Current Issue | Planned Change | Work Package | Risk |
|---|---|---|---|---|---|---|
| Tenant | `artifacts/api-server/src/services/marketplace.ts` | `parseTenantId` | Trusts client-supplied `tenantId` from request body/query — spoofable | Replace with `RequestContext.tenantId`, remove client-supplied path entirely | WP-00, WP-01 | High |
| Tenant | `artifacts/api-server/src/services/packageManagerService.ts` | tenant-filtered package queries (~L215 area) | Consumes `parseTenantId`'s spoofable value | Consume `RequestContext` instead | WP-00 | High |
| Tenant | `artifacts/api-server/src/middleware/*` (new file) | new tenant-context middleware | No middleware resolves/attaches tenant identity to `req` today | Add middleware after `adminAuth`/`internalAuth`/token-resolution, before route handlers | WP-01 | Medium |
| Tenant | `artifacts/api-server/src/middleware/internalAuth.ts` | `requireAuth`, `requireInternalRole` | No tenant resolution for internal-user sessions | Extend session→tenant resolution (internal user needs a tenant assignment) | WP-01 | Medium |
| Tenant | `artifacts/api-server/src/routes/public.ts` | `resolveToken` (~L44) | Resolves resource identity, not tenant | Extend to walk token→resource→tenant | WP-01 | Medium |
| Tenant | `artifacts/api-server/src/routes/public-review.ts` | token resolution path | Same gap as above for review-review flow | Same fix, review-specific resource chain | WP-01 | Medium |
| Tenant | `artifacts/api-server/src/services/jobWorkerService.ts` | `claimJob` (~L381-466), enqueue call sites | `payloadJson` has no `tenantId`; workers trust caller-provided ids | Add `tenantId` at enqueue time; never re-derive in worker | WP-01, WP-03 (Job/Worker first) | Medium |
| Tenant | `artifacts/api-server/src/services/creativeDocumentWorkerService.ts` | PDF export read-by-`projectDbId` (~L206-209) | No ownership/tenant check in worker itself | Rely on repository-layer read guard once migrated; do not duplicate checks per-worker | WP-06 (Project domain) | Medium |
| Tenant | `artifacts/api-server/src/services/creativePresentationWorkerService.ts` | PPTX export read-by-`projectDbId` (~L221-224) | Same as above | Same as above | WP-06 | Medium |
| Tenant | `artifacts/api-server/src/services/zipDeliveryService.ts` | `executeZipDeliveryJob` | Reads project by id, checks "unlocked" but not tenant | Repository-layer guard | WP-06 | Low-Medium |
| Tenant | `artifacts/api-server/src/services/aiSchedulerService.ts` | `tick`, `executeDueSchedules`, `executeTarget`, `createJobFromSchedule` (~L358-715) | Schedules have no tenant identity to propagate | Add `tenant_id` to `aiSchedulesTable`; propagate into created jobs/events | WP-01, DB plan §1.4 | Medium |
| Tenant | `artifacts/api-server/src/services/sseManager.ts` | channel registration keyed by `projectId` (~L5, L168), `registerSubscriber` (~L349-475) | No tenant check on subscription admission | Add tenant-match check between subscriber context and target project | WP-01/WP-06 | Medium |
| Tenant | `lib/db/src/schema/*.ts` (Project, Job/Worker, Human-task, Customer, Asset/brand files) | table definitions | No `tenant_id` column on most tables (see spec §1.1) | Add nullable `tenant_id`, backfill, later NOT NULL + index | WP-01 (schema step), WP-04 (hardening) | Medium |
| Repository | (new) `artifacts/api-server/src/repositories/*` | repository base class/pattern | Does not exist — all access is direct Drizzle queries per service file | Build tenant+soft-delete filtering, transaction support, typed errors, audit auto-emission | WP-05, WP-06 | Medium (foundational) |
| Repository | `artifacts/api-server/src/services/*.ts` (all migrated domains) | direct table query call sites | Every service function queries tables directly; no `RequestContext` parameter | Re-express as `(ctx, id, ...)`, route through repository | WP-05, WP-06 | Medium, mechanical but wide |
| Audit | `artifacts/api-server/src/services/aiAuditService.ts` | `logAudit` (L6-13) | Manual, per-call-site, "fire and forget" try/catch; only ~8 call sites cover a much larger set of mutations | Keep as fallback API for repository-bypass writes; primary path becomes automatic repository emission | WP-07, WP-08 | Low (schema), Medium (emission logic) |
| Audit | `lib/db/src/schema/ai-audit-logs.ts` | `aiAuditLogsTable` | Has `actorId` but no `tenantId`/`actorType`; rarely populated in practice | Add `tenant_id`, `actor_type`; revoke UPDATE/DELETE grants at DB layer | WP-07 | Low |
| Audit | `artifacts/api-server/src/services/zipDeliveryService.ts:305`, `jobWorkerService.ts:631`, `brandKitEnterpriseService.ts:282`, `assetLibraryService.ts:224`, `workerClusterService.ts:203`, `customer-workspace.ts:208`, `routes/agents.ts:41`, `routes/cp-review.ts:671` | existing manual `logAudit` calls | Working but not part of a systematic guarantee | Leave functioning; track as debt to eventually migrate onto repository-mediated writes where the underlying operation is repository-eligible | WP-08 (tracked, not urgent) | Low |
| Audit | `artifacts/api-server/src/services/canonicalEventService.ts` | `CanonicalEvent` projection (fields ~L104-125) | Risk of being confused with/merged into audit log during "converge on canonical model" work | Explicitly keep separate; document boundary; no code change required beyond the documentation in the main spec | — (documentation only) | Low |
| Audit | `lib/db/src/schema/ai-events.ts` | `aiEventsTable` | Risk of being confused with/repurposed as audit table | Explicitly keep as event-bus table only; no schema change | — (documentation only) | Low |
| Soft delete | `artifacts/api-server/src/services/packageManagerService.ts:215` | package removal handler | Hard `db.delete()` on `aiInstalledPackagesTable` | Replace with repository `softDelete` | WP-11 | Low |
| Soft delete | `artifacts/api-server/src/routes/workflows.ts:83` | `delete_workflow` route | Hard `db.delete()` on `aiWorkflowsTable` | Replace with `softDelete` | WP-11 | Low |
| Soft delete | `artifacts/api-server/src/services/seed.ts:227` | system reset | Hard `db.delete()` on `aiCapabilitiesTable` | Leave as-is (seed/reset utility, not a production user-facing delete) or explicitly document exemption | WP-11 (exemption decision) | Low |
| Soft delete | `artifacts/api-server/src/routes/registry.ts:138,305` | registry deletion routes | Hard `db.delete()` on `aiProvidersTable`, `aiModelsTable` | Replace with `softDelete` | WP-11 | Low |
| Soft delete | `artifacts/api-server/src/routes/prompts.ts:85` | `delete_prompt` route | Hard `db.delete()` on `aiPromptsTable` | Replace with `softDelete` | WP-11 | Low |
| Soft delete | `artifacts/api-server/src/routes/portfolio.ts:241,280,760` | catalog deletion routes | Hard `db.delete()` on `aiServicesTable`, `aiServicePackagesTable`, `aiServicePriceRulesTable` | Replace with `softDelete`, cascade where price rules are children of packages | WP-11, WP-10 (cascade) | Medium |
| Soft delete | `artifacts/api-server/src/routes/agents.ts:98,147` | `delete_agent` route | Hard `db.delete()` on `aiAgentsTable`, `aiAgentCapabilitiesTable` | Replace with `softDelete`, cascade capabilities under agent | WP-11, WP-10 (cascade) | Medium |
| Soft delete | `artifacts/api-server/src/routes/cp-review.ts:611` (and `:671` audit call) | client review rejection/cleanup path | Confirmed hard-delete in a customer-facing review flow | Replace with `softDelete`; highest customer-visible-impact site, migrate carefully | WP-11 (first or early in blast-radius order given customer visibility — re-sequence ahead of admin-only routes if review shows higher real-world risk) | Medium-High |
| Soft delete | `artifacts/api-server/src/services/cp-review-fixture.ts:44-50` | test fixture cleanup | Hard `db.delete()` across 5 tables | Explicitly exempt — test-only, leave as hard delete | — (documented exemption) | Low |
| Soft delete | `lib/db/src/schema/creative-projects.ts`, `ai-quotations.ts`, `ai-quotation-items.ts`, `creative-project-quotations.ts`, `ai-invoices.ts` | table definitions | No `deleted_at`/`deleted_by` columns despite being high-value business records | Add columns as forward-looking protection even without a current hard-delete call site | WP-09 (DB plan §3.1) | Low |
| Quotation | `artifacts/api-server/src/routes/quotations.ts` | legacy CRUD/issuance/feedback handlers (L104, L161-167, L212, L300, L380, L423) | Fully live creation path on the table being deprecated | Disable creation endpoints; keep read endpoints | WP-13 | Medium |
| Quotation | `artifacts/api-server/src/services/serviceRequestConversionService.ts` | `checkAndMaybeConvert` (legacy, L116-137, L314), `checkAndMaybeConvertByServiceQuotation` (canonical, L139-192, L326), common flow (L211-305) | Dual branch already exists and works; must not be collapsed prematurely | Document legacy branch as permanently read-only-forever; no structural change | WP-13 (documentation), no code change required | Low |
| Quotation | `artifacts/api-server/src/services/commercialGateService.ts` | dual `quotationId`/`serviceQuotationId` linkage | Already handles both; must stay dual | Verify continues to resolve both post-freeze; add regression test | WP-14 | Medium |
| Quotation | `artifacts/api-server/src/services/customerWorkspaceService.ts` | workspace quotation view (~L224) | Reads legacy quotations for workspace views | Extend/verify to resolve canonical source too for catalog-flow projects | WP-14 | Medium |
| Quotation | `artifacts/api-server/src/routes/public-review.ts:152` | public asset review page quotation status check | Reads legacy quotation status/totals directly | Check canonical `ai_quotations` first, fall back to legacy for pre-freeze projects | WP-14 | Medium-High (customer-facing) |
| Quotation | `artifacts/api-server/src/routes/customer-portal.ts:401` | `getCustomerReviewData` | Reads legacy quotation status directly | Same fix as above | WP-14 | Medium-High (customer-facing) |
| Quotation | `artifacts/api-server/src/routes/aiQuotations.ts`, `artifacts/api-server/src/services/aiQuotationService.ts` | canonical lifecycle (insert L85, update L148, status transitions L198/L246/L282, token access L228) | Already the intended canonical path; no defect | No change required beyond continuing to be the sole write target post-freeze | — | Low |
| Quotation | `artifacts/api-server/src/routes/catalog.ts:613,641` | auto-creates `ai_quotations` rows on service-request quoting | Already correct, canonical-only | No change required | — | Low |
| Quotation | `artifacts/ai-platform/src/hooks/use-catalog.ts`, `artifacts/ai-platform/src/pages/service-requests.tsx:203` | frontend quotation issuance/consumption | Already targets canonical `/api/public/quotations/` and catalog issue-quotation endpoint | No change required; verify no legacy-creation call remains before treating WP-13 as complete | WP-13 (verification step) | Low |
| Quotation | `lib/db/src/schema/ai-quotations.ts`, `ai-quotation-items.ts`, `creative-project-quotations.ts` | table definitions | `ai_quotations.tenantId` nullable, no other quotation-schema tenant work needed | Follow standard tenant-column lifecycle (backfill→enforce→NOT NULL→RLS) for `ai_quotations` only | WP-01/WP-03/WP-04 (Commercial/Quotation, last) | Medium |
