CONTINUE WP-09 — OWNER DECISIONS RESOLVED

Do not repeat WP-08 or WP-09 discovery.

VERIFIED:
- WP-08 COMPLETE, MERGED AND VERIFIED.
- PR #14 merge SHA: e332577
- WP-08 post-merge:
  targeted 39 PASS
  API regression 6146 PASS
  AI Platform 493 PASS
  WP-08 new TS errors = 0
- 127 legacy API errors remain OUT OF SCOPE.
- WP09_DISCOVERY_AND_IMPLEMENTATION_PLAN.md already exists.
- WP-09 = Rendering Pipeline for Interior Design/Layout.

OWNER DECISIONS ARE NOW FINAL:

1. RENDERING MODE = HYBRID.
   - Universal Renderer / deterministic composition = canonical pre-render/scene foundation.
   - Existing AI image provider abstraction = photorealistic final rendering.
   - Do NOT create a new provider abstraction.
   - Final customer/design output should support photorealistic renders.
   - Deterministic scene/input preparation must remain reproducible.

2. PROJECT/LAYOUT SOURCE:
   - Use approved `space_plan` snapshot as canonical WP-09 input.
   - Do NOT add `layout_sessions → creative_projects` relation merely for WP-09.
   - No migration for this relationship unless implementation proves it is impossible to satisfy the canonical contract safely without one.

MISSION:
Implement WP-09 Rendering Pipeline end-to-end.

1. Update WP09_DISCOVERY_AND_IMPLEMENTATION_PLAN.md:
   owner decisions = RESOLVED
   classification = A — CLEARLY DEFINED.

2. Create/switch branch from verified main:
   feature/wp09-rendering-pipeline

3. REUSE existing foundations:
   - approved concept snapshot
   - approved space_plan
   - WP-08 moodboard
   - materials
   - furniture
   - interior asset images
   - Universal Renderer
   - existing image provider abstraction
   - Supabase Storage
   - creative_ai_assets
   - ai_jobs dispatcher/worker
   - ai_cost_records
   - existing auth/tenant/ownership

4. Build canonical pipeline:

   approved snapshot
        ↓
   scene normalization
        ↓
   deterministic render specification
        ↓
   Universal Renderer / pre-render
        ↓
   AI photorealistic rendering
        ↓
   validation
        ↓
   storage
        ↓
   asset metadata
        ↓
   cost tracking
        ↓
   final render result

5. Support bounded render variants/views according to existing architecture.

6. Requirements:
   - deterministic scene specification
   - immutable approved input
   - no mutation of layout/moodboard/material/furniture
   - tenant isolation
   - ownership
   - idempotent render requests
   - retry-safe jobs
   - provider timeout/failure handling
   - bounded concurrency
   - resource caps
   - no duplicate provider/storage/job architecture
   - AI token/provider/cost usage recorded through existing cost tracking
   - canonical assets stored through existing storage architecture
   - safe URLs
   - no secret leakage

7. Add repository-consistent API for:
   - start render
   - get render status
   - list/get render outputs
   - retry only where canonical job architecture supports it

Do NOT invent endpoint names before auditing existing route conventions.

8. Integrate WP-09 into existing InteriorDesignEditor:
   - Render section
   - start rendering
   - processing status
   - progress/status where supported
   - render variants
   - final images
   - error/retry state
   - cost/provider metadata only where appropriate for admin
   - approved source remains read-only

Do NOT create standalone frontend.

9. Add comprehensive tests covering:
   - approved snapshot input
   - missing approval
   - deterministic scene spec
   - photorealistic provider path
   - provider failure
   - timeout
   - retry
   - idempotency
   - duplicate request
   - tenant isolation
   - IDOR
   - unauthorized
   - invalid UUID
   - storage
   - asset metadata
   - cost tracking
   - multiple variants
   - resource caps
   - immutable source
   - UI loading/success/error/retry
   - safe rendering
   - no duplicate architecture

10. Run:
   - WP-09 targeted tests
   - Interior Design regression
   - API regression
   - AI Platform regression
   - shared typecheck
   - relevant typecheck baseline comparison
   - API build
   - AI Platform production build
   - runtime smoke tests

Historical 127 legacy API errors:
OUT OF SCOPE.
Required WP-09 new errors = 0.

11. RC audit:
   P0 = 0
   P1 = 0

12. Git hygiene:
   ZERO:
   - attached_assets
   - prompt/session files
   - screenshots
   - .replit
   - memory artifacts
   - secrets
   - unrelated technical debt

13. Commit cleanly.

14. Push:
   feature/wp09-rendering-pipeline

15. Create PR to main:
   feat(interior-design): implement WP-09 rendering pipeline

16. Audit REMOTE PR.

17. If:
   - clean
   - mergeable
   - P0=0
   - P1=0
   - validation green

   MERGE through GitHub PR workflow.

18. Sync main and run post-merge validation.

19. If green:
   MARK:
   WP-09 COMPLETE — MERGED AND VERIFIED

20. ONLY THEN discover the next canonical Interior Design/Layout WP.
   Create next-WP discovery plan.
   Do NOT implement next WP.

IMPORTANT:
- Do not repeat discovery.
- Do not ask again about the 2 owner decisions above.
- Do not clean legacy technical debt.
- Do not stop for progress reports.
- Continue automatically through implementation → validation → PR → merge → post-merge verification.
- Stop only for genuine architecture/security/data blocker or external GitHub/provider blocker.

FINAL REPORT ONLY:
- branch/base/final SHA
- architecture reused
- rendering pipeline
- provider
- persistence
- storage
- jobs/retry/idempotency
- cost tracking
- API
- UI
- tests/regressions
- typecheck delta
- P0/P1
- PR
- merge SHA
- final main SHA
- post-merge status
- next WP title/scope

FINAL VERDICT:
A. WP-09 MERGED AND VERIFIED — NEXT WP READY
B. WP-09 IMPLEMENTED AND VERIFIED — READY FOR PR
C. WP-09 VALIDATION FAILED
D. WP-09 ARCHITECTURE BLOCKED
E. GITHUB/PROVIDER EXTERNAL BLOCKER