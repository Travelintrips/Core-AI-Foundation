Anda adalah Principal Software Architect untuk Batch C Creative Enterprise.

BRANCH YANG DITANGANI:

1. feature/11-template-matching
2. feature/14-rendering-engine
3. feature/15-graphic-design
4. feature/16-presentation-document

Tujuan utama:

Menghilangkan duplicate engines dan mengubah implementation menjadi adapter
terhadap source of truth yang sudah ada.

Kerjakan satu branch pada satu waktu:

git fetch origin --prune
git switch <branch>
git pull --ff-only origin <branch>
git branch --show-current
git status

Selesaikan branch, test, commit, push, pastikan clean,
baru pindah ke branch berikutnya.

DILARANG:

- membuat engine universal baru
- membuat source of truth kedua
- mengubah routes/index.ts
- mengubah App.tsx
- mengubah package.json atau pnpm-lock.yaml
- mengubah shared adminAuth.ts
- mengubah seedCatalog.ts atau seedTemplates.ts
- merge ke main

Jika wiring diperlukan, tulis ke integration manifest.

==================================================
TEAM 11 — TEMPLATE MATCHING
==================================================

Audit designTemplateService existing.

Team 11 harus menjadi intelligence adapter yang:

- mengambil candidate dari existing template service
- menambahkan scoring
- menambahkan explanation
- menerapkan deterministic tie-breaking
- tidak menyimpan template registry sendiri
- tidak menjadi matching engine kedua

Perbaiki performa:

- filter candidate via database
- domain/service/style/industry/tag filters
- hard candidate limit
- jangan membandingkan seluruh template in-memory

Tambahkan:

- rate limiting
- brief-size limit
- private-template protection
- no-compatible-template behavior

TEST:

- adapter existing dipanggil
- large library tidak full scan
- stable tie breaking
- private template tidak bocor
- rate limit

==================================================
TEAM 14 — RENDERING ENGINE
==================================================

Revert locked-file changes.

Audit existing design renderer.

Team 14 harus:

- memakai existing renderer sebagai source of truth
- hanya menambahkan adapter/capability yang belum tersedia
- tidak membuat universal renderer kedua

Tambahkan security/resource controls:

- secure fetch untuk image/font URL
- SSRF blocking
- max canvas dimensions
- max asset count
- max input bytes
- max embedded asset bytes
- max output size
- render timeout
- MIME validation

Tambahkan idempotency:

- canonical content hash
- request yang sama mengembalikan artifact existing
- jangan membuat duplicate storage output

TEST:

- existing renderer dipanggil
- SSRF ditolak
- oversized input ditolak
- same content hash reuse artifact
- invalid output tidak disimpan

==================================================
TEAM 15 — GRAPHIC DESIGN
==================================================

Revert locked-file changes.

Audit existing Design Studio dan canvas engine.

Team 15 hanya boleh menyediakan:

- graphic-design brief mapping
- service-specific blueprint selection
- domain QC
- output/deliverable manifest
- adapter ke existing design/canvas engine

Jangan membuat design engine kedua.

Perbaiki path traversal:

- abaikan filename/path dari user
- gunakan generated UUID
- fixed storage prefix
- trusted extension
- normalize dan containment check

Pastikan semua execution melalui satu canonical job adapter.

Tambahkan pagination pada job list.

TEST:

- ../../ input tidak memengaruhi output path
- user filename tidak digunakan
- output tetap di allowed directory
- canonical adapter selalu dipanggil
- job list paginated

==================================================
TEAM 16 — PRESENTATION & DOCUMENT
==================================================

Revert locked-file changes.

Gunakan existing:

- Presentation Engine
- Document Engine

Team 16 hanya menjadi adapter untuk:

- brief mapping
- section/slide mapping
- page policy
- anti-fabrication policy
- domain QC
- deliverable manifest

Jangan membuat PPTX/PDF engine kedua.

Tambahkan:

- secure external image handling
- max slides
- max pages
- max images
- max image bytes
- max generated bytes
- generation timeout

Pertahankan anti-fabrication:
jangan membuat metrics, finance, team, competitor, atau market claims tanpa data.

TEST:

- existing engines dipanggil
- private URL ditolak
- slide/page limit
- oversized image ditolak
- anti-fabrication skip tetap bekerja
- output validation

==================================================
OUTPUT
==================================================

Laporkan per branch:

- duplicate implementation yang ditemukan
- kode yang dihapus atau dijadikan adapter
- canonical engine yang dipakai
- locked files yang direvert
- tests
- typecheck
- commit hash
- push status
- READY_FOR_REAUDIT atau NOT_READY