CONTINUE WP-11 FROM CURRENT STATE — DO NOT REPEAT WP-10 OR REDISCOVER WP-11.

Repository:
Travelintrips/Core-AI-Foundation

CURRENT VERIFIED STATE:

* WP-10 = COMPLETE — MERGED AND VERIFIED
* WP-10 merge SHA = `81edb468...`
* local main = origin/main
* post-merge validation PASS
* canonical successor = WP-11 — Export Engine
* WP-11 scope = CLEARLY DEFINED
* existing export/storage/PDF/ZIP/job foundations already audited
* no owner decision currently blocks implementation

MISSION:
Implement WP-11 Export Engine end-to-end and finish its full release lifecycle.

DO NOT:

* return to WP-10
* repeat WP-11 discovery
* reopen owner decisions
* create duplicate storage/job/PDF/ZIP infrastructure
* clean legacy API TypeScript debt
* commit prompts/screenshots/attached_assets/.replit/memory/session/secrets
* start WP-12 implementation

USE CURRENT WP-11 PLAN AND WORKING TREE AS SOURCE OF TRUTH.

==================================================

1. CREATE/VERIFY WP-11 BRANCH
   ==================================================

Use verified post-WP-10 `main`.

Branch:

`feature/wp11-export-engine`

If it already exists, continue from it.
Do not recreate valid work.

==================================================
2. REUSE EXISTING FOUNDATIONS
=============================

REUSE FIRST:

* Interior Design project aggregate
* approved concept snapshot
* approved space_plan
* moodboard
* rendering outputs from WP-09
* review/version snapshots from WP-10
* canonical asset/image storage
* Supabase Storage
* existing PDF generation
* existing ZIP/archive utilities
* existing job/queue infrastructure
* existing tenant/auth/ownership
* existing audit/event systems

Do NOT create parallel export infrastructure.

==================================================
3. WP-11 OBJECTIVE
==================

Build canonical export packages for Interior Design.

Export must support repository-authoritative formats such as:

* project/design summary
* approved concept
* layout/space plan
* moodboard
* material references
* furniture references
* render images
* version/review metadata
* PDF output
* ZIP package where applicable

Use actual roadmap/plan definitions already discovered as authority.

==================================================
4. EXPORT SOURCE RULE
=====================

Exports must use canonical server-side data.

Prefer approved/reviewed version boundaries.

Do NOT trust client-submitted design content as export authority.

Historical/version export must preserve version identity.

No mutation of approved design state.

==================================================
5. EXPORT MANIFEST
==================

Create a deterministic export manifest containing canonical equivalents of:

* project ID
* tenant ID
* export ID
* source version ID
* project metadata
* generated timestamp
* export format
* included sections/assets
* asset IDs/paths
* checksums/hashes where existing architecture supports them
* generation status
* warnings
* metadata

Do not embed secrets or unnecessary PII.

==================================================
6. PDF EXPORT
=============

Reuse existing PDF infrastructure.

Generate a professional Interior Design export PDF using canonical project data.

Include only sections supported by available data, such as:

* title/project summary
* design concept
* approved layout
* moodboard
* palette
* materials
* furniture
* final renders
* reviewer/version metadata

Requirements:

* safe rendering
* deterministic section ordering
* missing sections handled gracefully
* no binary/base64 duplication inside persisted snapshots
* correct tenant/project ownership

==================================================
7. ZIP EXPORT PACKAGE
=====================

If WP-11 roadmap requires package download:

reuse existing ZIP/archive utilities.

Package may include:

* PDF
* final render files
* selected asset files
* machine-readable manifest JSON
* approved design metadata

Do NOT duplicate stored files permanently merely to produce ZIP.

Prefer streaming/temp-generation/canonical storage patterns already used by repository.

==================================================
8. JOB / EXPORT LIFECYCLE
=========================

If exports are asynchronous:

reuse existing job/queue infrastructure.

Support:

* queued
* processing
* completed
* failed

Requirements:

* idempotent export requests
* duplicate request protection
* safe retry
* bounded concurrency
* tenant-scoped jobs
* resource caps
* failure reason sanitization

Do not create a new queue.

==================================================
9. STORAGE
==========

Reuse canonical storage.

Persist export artifacts only if required by existing architecture.

Requirements:

* tenant/project scoped path
* safe filenames
* no traversal
* controlled MIME/content type
* canonical asset metadata
* cleanup/retention according to existing policy
* no secret leakage

==================================================
10. API
=======

Use existing Interior Design route conventions.

Implement repository-consistent operations for:

* create/start export
* get export status
* list export artifacts
* download/get export result where architecture permits
* retry only if canonical job lifecycle supports it

Requirements:

* admin auth
* UUID validation
* tenant isolation
* IDOR protection
* ownership
* request/response schema
* bounded payload
* structured errors
* no client-trusted tenant IDs

==================================================
11. UI
======

Integrate Export into existing `InteriorDesignEditor`.

No standalone frontend.

Add Export section/panel with:

* export format
* included sections
* Start Export
* progress/status
* completed artifacts
* PDF download/open action
* ZIP download/open action where supported
* failure/retry
* version/source information

Approved/history source must remain read-only.

==================================================
12. VERSION INTEGRATION
=======================

Integrate WP-10 versioning.

Export must be able to identify which immutable design version was exported.

Where applicable:

* export current approved version
* export selected historical version
* preserve version ID in manifest
* never rewrite historical version

==================================================
13. SECURITY
============

Verify:

* authentication
* tenant isolation
* ownership
* IDOR
* safe storage paths
* safe filenames
* no arbitrary filesystem access
* no arbitrary remote URL fetch
* no secret leakage
* no unnecessary PII
* bounded export size
* bounded file count
* safe MIME types
* safe error messages

==================================================
14. RESOURCE LIMITS
===================

Apply bounded limits for:

* number of images
* file sizes
* archive size
* PDF pages/sections where relevant
* number of simultaneous exports
* timeout
* retries

Use existing platform limits where available.

==================================================
15. TEST MATRIX
===============

Add comprehensive WP-11 tests covering at minimum:

1. valid PDF export
2. valid package/ZIP export if supported
3. approved version export
4. historical version export
5. invalid UUID
6. unauthorized
7. wrong tenant
8. IDOR
9. project not found
10. version not found
11. missing optional sections
12. deterministic manifest
13. deterministic section ordering
14. safe filename
15. storage path safety
16. asset inclusion
17. render inclusion
18. moodboard inclusion
19. material inclusion
20. furniture inclusion
21. no mutation
22. duplicate/idempotent request
23. retry
24. job failure
25. resource cap
26. oversized export rejection
27. safe MIME
28. no secret/PII leakage
29. UI loading
30. UI success
31. UI error
32. UI retry
33. download/result handling
34. version linkage
35. audit/event linkage where required

No skip/todo/only.

==================================================
16. VALIDATION
==============

Run:

* WP-11 targeted tests
* Interior Design regression
* API regression
* AI Platform regression
* shared library typecheck
* WP-11 TypeScript delta
* API build
* AI Platform typecheck
* AI Platform production build
* runtime smoke

Required:

* WP-11 new errors = 0
* P0 = 0
* P1 = 0
* legacy API TypeScript debt remains OUT OF SCOPE

==================================================
17. RC AUDIT
============

Audit:

* architecture reuse
* version integrity
* PDF correctness
* ZIP/package safety
* storage
* job lifecycle
* idempotency
* retry
* tenant isolation
* IDOR
* security
* resource limits
* UI
* tests
* git hygiene

Fix genuine WP-11 P0/P1 findings automatically.

Re-run affected gates.

==================================================
18. GIT HYGIENE
===============

ZERO allowed:

* attached_assets
* uploaded prompts
* screenshots
* `.replit`
* memory/session artifacts
* secrets
* temporary export files
* generated sample PDFs/ZIPs unless repository policy explicitly tracks fixtures
* unrelated technical debt

Run `git diff --check`.

==================================================
19. COMMIT / PUSH
=================

Commit WP-11 cleanly.

Push:

`feature/wp11-export-engine`

Verify:

local SHA = remote SHA

==================================================
20. CREATE PR
=============

Create PR to `main`.

Title:

`feat(interior-design): implement WP-11 export engine`

Audit REMOTE PR.

Required:

* correct base/head
* expected scope only
* no contamination
* mergeable
* tests green
* P0 = 0
* P1 = 0

==================================================
21. MERGE
=========

If all release gates PASS:

MERGE through GitHub PR workflow.

Never direct-push to `main`.

Record PR and merge SHA.

==================================================
22. POST-MERGE
==============

Fetch origin.

Sync local `main = origin/main`.

Verify WP-11 ancestry.

Run post-merge:

* WP-11 targeted
* Interior Design regression
* API regression
* AI Platform regression
* shared typecheck
* WP-11 TS delta
* API build
* AI Platform build
* runtime smoke

Required:

new errors = 0
P0 = 0
P1 = 0

If PASS:

`WP-11 COMPLETE — MERGED AND VERIFIED`

==================================================
23. DISCOVER WP-12 ONLY
=======================

After WP-11 completes:

discover ONLY canonical successor in Interior Design/Layout roadmap.

Create:

`NEXT_WP_DISCOVERY_AND_IMPLEMENTATION_PLAN.md`

Determine:

* WP-12 canonical title
* objective
* dependencies
* existing foundations
* implementation gaps
* backend/UI/persistence scope
* security
* tests
* migrations
* owner decisions
* classification

If clearly defined:

create/push its feature branch from verified main.

DO NOT IMPLEMENT WP-12.

==================================================
EXECUTION RULE
==============

Do not stop for progress reports.

Continue automatically:

implementation
→ tests
→ validation
→ RC audit
→ cleanup
→ commit
→ push
→ PR
→ remote audit
→ merge
→ post-merge
→ WP-12 discovery.

STOP only for:

1. genuine GitHub/external blocker
2. unresolved architecture/security/data owner decision
3. destructive migration requiring approval
4. unrecoverable validation failure
5. canonical roadmap conflict

FINAL REPORT ONLY:

WP-11

* base SHA
* branch
* final feature SHA
* files changed
* export formats
* manifest
* PDF
* ZIP/package
* storage
* job lifecycle
* idempotency/retry
* version integration
* API
* UI
* security
* targeted tests
* regression totals
* TS delta
* P0/P1
* PR number/URL
* merge SHA
* final main SHA
* post-merge validation
* final status

WP-12

* canonical title
* objective
* classification
* existing foundations
* owner decisions
* plan path
* branch
* implementation started = NO

FINAL VERDICT:

A. WP-11 MERGED AND VERIFIED — WP-12 READY

B. WP-11 IMPLEMENTED AND VERIFIED — READY FOR PR

C. WP-11 VALIDATION FAILED

D. WP-11 ARCHITECTURE/OWNER DECISION BLOCKED

E. GITHUB/EXTERNAL BLOCKER
