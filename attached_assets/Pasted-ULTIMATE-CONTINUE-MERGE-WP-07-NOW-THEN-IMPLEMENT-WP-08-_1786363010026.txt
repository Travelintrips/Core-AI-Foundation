ULTIMATE CONTINUE — MERGE WP-07 NOW, THEN IMPLEMENT WP-08 MOODBOARD GENERATOR

Repository:
Travelintrips/Core-AI-Foundation

CURRENT VERIFIED STATE:

WP-07:
Layout Constraint Engine

Clean remote branch:
feature/wp07-layout-constraint-engine

Clean commit:
afa049c

Existing GitHub PR:
#11
Title:
feat(layout): implement WP-07 layout constraint engine

Verified PR scope includes:

- deterministic whole-layout evaluation
- 11 hard constraints
- 9 soft constraints
- score 0–100
- canonical WP-03B/WP-04 geometry reuse
- endpoint:
  POST /api/ai/layout-sessions/:sessionId/constraints/evaluate
- WP-06B Placement Canvas integration
- tenant isolation
- non-disclosure 404
- no mutation
- no LLM/provider
- bounded pair checks
- approved layout remains immutable
- targeted tests green
- full regression green
- P0 = 0
- P1 = 0

NEXT AUTHORITATIVE WP:

WP-08 — Moodboard Generator

IMPORTANT:

Do not reopen WP-07 namespace.
Do not return to WP-06.
Do not clean TD-01..TD-04 legacy typecheck debt.
Do not mix secret rotation into feature branches.
Do not implement WP-09.

MISSION:

A. Merge WP-07 cleanly
B. Verify main
C. Immediately implement WP-08
D. Validate WP-08
E. Push / PR / review / merge WP-08
F. Verify main again
G. Discover WP-09 only

==================================================
PART A — FINALIZE WP-07
==================================================

PHASE A1 — VERIFY PR #11

Verify:

- PR #11 exists
- open
- not draft
- base = main
- head = feature/wp07-layout-constraint-engine
- head SHA = afa049c or verified clean descendant
- changed files only WP-07 scope
- no attached_assets
- no prompt/session files
- no screenshots
- no .replit
- mergeability
- required checks

If mergeability is pending:
wait/recheck.

If merge conflict appears:
resolve against current origin/main carefully,
preserve newer main changes,
rerun validation.

==================================================
PHASE A2 — FINAL WP-07 VALIDATION
==================================================

Run actual current commands supported by repo:

pnpm run typecheck:libs
pnpm --filter api-server run build
pnpm --filter api-server test

AI Platform:

- typecheck
- tests
- production build

Run WP-07 targeted tests.

Required:

- all targeted tests pass
- API regression pass
- AI Platform regression pass
- build pass
- no new WP-07 type errors
- P0 = 0
- P1 = 0

Known legacy API typecheck baseline remains separate.

==================================================
PHASE A3 — MERGE PR #11
==================================================

If all gates pass:

merge PR #11 using repository-standard merge method.

Preferred:
standard merge commit unless repo policy differs.

Record:

- merge SHA
- merged_at
- merged_by

Do not replace PR merge with direct push to main.

==================================================
PHASE A4 — VERIFY MAIN
==================================================

Run:

git fetch origin main
git switch main
git reset --hard origin/main

Verify:

local main SHA =
origin/main SHA =
remote main SHA

Run again:

- library typecheck
- API build
- API regression
- AI Platform typecheck
- AI Platform tests
- AI Platform production build
- WP-07 targeted tests

If green:

MARK WP-07 COMPLETE.

==================================================
PART B — WP-08 AUTHORITATIVE CONFIRMATION
==================================================

WP-08 active title:

Moodboard Generator

Roadmap:
Interior Design / Layout

Verify repository references only to determine exact detailed scope.

Do not reopen the WP number.

Determine:

- generation mode
- persistence model
- API placement
- UI location
- allowed image-provider behavior
- approved-state behavior

If the repository clearly provides answers:
continue automatically.

If one unresolved decision would materially change:
- DB schema
- provider usage
- public access
- approved-state mutation
- breaking API

then STOP and report owner decision.

For minor choices:
choose safest backward-compatible default.

==================================================
PART C — CREATE WP-08 BRANCH
==================================================

Create from verified origin/main:

feature/wp08-moodboard-generator

Never implement on main.

==================================================
PART D — AUDIT EXISTING MOODBOARD FOUNDATIONS
==================================================

Before coding search for:

moodboard
visual concept
interior images
asset images
materials
furniture
color palette
design concept
image generation
image provider
storage
ai-assets
id_interior_asset_images

Reuse existing services and contracts first.

Do not create duplicate:

- image storage
- material catalog
- furniture catalog
- color system
- image-provider abstraction

==================================================
PART E — WP-08 CORE CONTRACTS
==================================================

Implement shared typed/Zod contracts using repository conventions.

Required conceptual types:

MoodboardGenerateRequest
MoodboardResult
MoodboardPalette
MoodboardMaterialItem
MoodboardFurnitureItem
MoodboardImageItem
MoodboardSection
MoodboardGenerationMetadata

Request should use canonical identifiers and project context.

Response should include:

- moodboardId
- projectUuid
- style
- title
- colorPalette
- materials
- furniture
- referenceImages
- sections
- warnings
- status
- metadata

Avoid raw provider responses.

==================================================
PART F — GENERATION STRATEGY
==================================================

Use authoritative repository scope.

Preferred safe design if repository permits:

STRUCTURED MOODBOARD FIRST

Compose from:

- current concept draft
- style
- WP-05 material recommendations/selections
- Furniture Library
- interior asset images
- user/admin uploads
- existing approved references

If repository explicitly requires AI image generation:

reuse existing image provider abstraction.

Do NOT introduce a new provider.

Keep structured moodboard result canonical even if optional visual
generation is nondeterministic.

==================================================
PART G — COLOR PALETTE
==================================================

Generate deterministic structured palette from canonical design data.

Support repository-equivalent roles:

- primary
- secondary
- accent
- neutral/background

Requirements:

- finite structured values
- deterministic ordering
- explainable source
- no source mutation

==================================================
PART H — MATERIAL INTEGRATION
==================================================

Use WP-05 canonical material data.

Moodboard material items should reference canonical material IDs.

Include available:

- name
- category
- color
- texture
- finish
- material image
- technical attributes needed for display

Do not duplicate database material rows.

==================================================
PART I — FURNITURE INTEGRATION
==================================================

Use canonical Furniture Library records.

Include:

- furniture ID
- name
- category
- style/finish
- image when available

Do not alter WP-06 placements.

Moodboard is advisory/visual.

==================================================
PART J — IMAGE ASSET INTEGRATION
==================================================

Reuse canonical image/storage architecture.

Known environment includes:

- Supabase Storage
- ai-assets bucket
- existing interior asset images

Use existing image service.

Requirements:

- tenant/project scope
- safe URL
- attribution/source metadata where applicable
- manual-upload protection
- no duplicate upload on repeated generation where unnecessary
- no raw secret exposure

==================================================
PART K — PERSISTENCE
==================================================

Audit existing schema before changing anything.

Prefer:

existing project/draft JSONB or existing moodboard-compatible storage

if canonical architecture already provides it.

Create a new migration/table ONLY if authoritative WP-08 roadmap explicitly requires persistent moodboard versions and no existing model is adequate.

Do not invent parallel persistence.

==================================================
PART L — SERVICE
==================================================

Implement repository-consistent service:

moodboardService.ts

or existing equivalent location.

Responsibilities:

- resolve project
- tenant/ownership guard
- resolve concept draft
- collect style/context
- collect materials
- collect furniture
- collect images
- construct palette
- construct structured moodboard
- optionally call canonical image generation abstraction if required
- return safe deterministic structured result
- preserve source/input immutability

==================================================
PART M — API
==================================================

Implement repository-consistent authenticated admin API.

Expected canonical equivalent of:

POST /api/ai/interior-design/projects/:projectUuid/moodboard/generate

GET /api/ai/interior-design/projects/:projectUuid/moodboard

Requirements:

- admin auth
- UUID validation
- project tenant scope
- ownership enforcement
- Zod request validation
- Zod response validation
- resource caps
- structured errors
- no raw provider response
- no secrets
- approved state policy respected

==================================================
PART N — UI
==================================================

Integrate into the EXISTING Interior Design Editor.

Add Moodboard section/tab/panel.

Render:

- moodboard title/style
- color palette
- materials
- furniture
- image references
- sections
- warning state
- generation state

Actions if in authoritative scope:

- Generate Moodboard
- Regenerate
- refresh/reload

Approved state:

read-only according to canonical concept lifecycle.

Do not create a standalone UI.

==================================================
PART O — SECURITY
==================================================

Verify:

- admin auth
- tenant isolation
- IDOR
- ownership
- safe image URLs
- safe React rendering
- no innerHTML injection
- result allowlists
- resource caps
- image-provider secrets server-only
- no storage path traversal
- no cross-project images

==================================================
PART P — PERFORMANCE
==================================================

Use bulk reads.

Avoid N+1.

Bound:

- material item count
- furniture item count
- reference image count

Record diagnostic:

- materials loaded
- furniture loaded
- images loaded
- DB query count where practical
- elapsedMs

No flaky hard timing assertion.

==================================================
PART Q — TEST MATRIX
==================================================

Implement comprehensive tests including:

1. valid moodboard
2. invalid project UUID
3. unauthorized
4. wrong tenant
5. project not found
6. missing concept
7. style resolved
8. palette generated
9. material inclusion
10. furniture inclusion
11. image inclusion
12. missing image fallback
13. manual image protection
14. deterministic structured result
15. input immutability
16. empty materials
17. empty furniture
18. partial images
19. warnings
20. safe URLs
21. approved-state behavior
22. ownership
23. no duplicate material
24. no duplicate furniture
25. no duplicate storage upload
26. request schema
27. response schema
28. UI loading
29. UI error
30. UI success
31. safe rendering
32. palette UI
33. material cards
34. furniture cards
35. image attribution
36. regeneration
37. repeated calls
38. resource caps
39. no N+1
40. representative large moodboard

If image generation exists also test:

- provider abstraction
- provider failure
- fallback
- no provider secrets in response

No skip/todo/only.

==================================================
PART R — DOCUMENTATION
==================================================

Create:

docs/wp08-moodboard-generator.md

Document:

- objective
- architecture
- generation mode
- structured result
- color palette
- material integration
- furniture integration
- images/storage
- persistence
- API
- UI
- security
- deterministic boundary
- performance
- known limitations
- WP-09 boundary

==================================================
PART S — VALIDATION
==================================================

Run targeted WP-08 tests.

Then:

pnpm run typecheck:libs
pnpm --filter api-server run build
pnpm --filter api-server test

AI Platform:

- typecheck
- tests
- production build

Required:

- all new tests pass
- API regression pass
- AI Platform regression pass
- no new TypeScript errors
- build pass

Legacy TD-01..TD-04 stays separate.

==================================================
PART T — WP-08 RC AUDIT
==================================================

Audit:

- scope
- canonical reuse
- DB/persistence
- tenant/ownership
- storage
- image safety
- API
- UI
- determinism
- resource bounds
- performance
- tests
- git hygiene

Classify:

P0
P1
P2
P3

Required before PR:

P0 = 0
P1 = 0

Fix genuine WP-08 blockers only.

==================================================
PART U — GIT HYGIENE
==================================================

Forbidden:

attached_assets
prompt/session files
screenshots
.replit
raw secrets
unrelated TD cleanup
unrelated features

Create atomic commits, e.g.:

feat(moodboard): add WP-08 moodboard engine
feat(moodboard): add moodboard API and editor integration
test(moodboard): add WP-08 coverage
docs(moodboard): document WP-08

==================================================
PART V — PUSH + PR + REVIEW + MERGE
==================================================

Push branch:

feature/wp08-moodboard-generator

Create PR to main.

Title:

feat(moodboard): implement WP-08 moodboard generator

Final review requires:

- merge-base clean
- P0 = 0
- P1 = 0
- checks green
- no accidental files
- no secrets
- no WP-07 regression

If clean:

merge using repository-standard method.

==================================================
PART W — POST-MERGE WP-08
==================================================

Sync local main to origin/main.

Verify SHA equality.

Run full validation again.

If all green:

MARK WP-08 COMPLETE.

==================================================
PART X — DISCOVER WP-09
==================================================

Search only authoritative Interior Design/Layout roadmap for:

WP-09
WP09
next work package
next milestone

Create:

WP09_DISCOVERY_AND_IMPLEMENTATION_PLAN.md

If clearly defined:
create WP-09 branch.

Do NOT implement WP-09.

==================================================
FINAL REPORT
==================================================

WP-07
1. PR #11 final state
2. WP-07 merge SHA
3. Main SHA after WP-07
4. WP-07 post-merge validation

WP-08
5. Title
6. Authoritative scope
7. Branch
8. Base SHA
9. Final feature SHA
10. Generation mode
11. Persistence
12. API
13. UI
14. Palette
15. Materials
16. Furniture
17. Image/storage integration
18. Security
19. Test count
20. API regression
21. AI Platform regression
22. Typecheck delta
23. Build
24. P0
25. P1
26. PR number
27. PR URL
28. Merge SHA
29. Main SHA after WP-08
30. Post-merge validation
31. WP-08 final status

WP-09
32. Title
33. Scope classification
34. Owner decisions
35. Branch created
36. Plan path

Use exactly one verdict:

A.
WP-07 AND WP-08 MERGED AND VERIFIED — WP-09 READY

B.
WP-07 MERGED — WP-08 OWNER DECISION REQUIRED

C.
WP-08 IMPLEMENTED — EXTERNAL/GITHUB BLOCKER

D.
WP-08 VALIDATION FAILED

E.
WP-07 FINALIZATION FAILED

F.
WP-08 IMPLEMENTATION BLOCKED

IMPORTANT:

Do not stop after WP-07 if WP-08 scope is confirmed.
Do not ask for another routine prompt.
Complete WP-08 lifecycle automatically.
Do not implement WP-09.