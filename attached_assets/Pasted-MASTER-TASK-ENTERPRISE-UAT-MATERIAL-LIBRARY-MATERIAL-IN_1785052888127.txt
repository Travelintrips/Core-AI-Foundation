MASTER TASK — ENTERPRISE UAT
MATERIAL LIBRARY + MATERIAL INTELLIGENCE + MATERIAL CATALOG FOUNDATION

Repository:
Travelintrips/Core-AI-Foundation

Target integration branch:
integration/material-intelligence-v2

Verified Phase 3 source branch:
feature/material-phase3-catalog-foundation

Verified Phase 3 remote HEAD:
c536f2287c3887103e6a1a2929661ac70a850ca6

Original Phase 3 foundation commit:
5eb3984

OBJECTIVE

Create or update the integration branch, combine the verified Phase 2 implementation with the verified Phase 3 Foundation, then perform full Enterprise UAT.

Do not merge to main.
Do not activate Phase 3.
Do not begin Phase 4.
Do not mount the Phase 3 route.
Do not register any catalog provider at production startup.

========================================================
PHASE 0 — REPOSITORY AND BRANCH VERIFICATION
========================================================

First report:

- repository root
- git remote -v
- current branch
- current HEAD
- git status --short
- local and remote branches
- whether the clone is shallow
- latest 20 commits

Confirm the repository is:

Travelintrips/Core-AI-Foundation

Fetch the remote branches using existing secure GitHub authentication.

Do not print credentials or tokens.

Verify these objects are available:

- Phase 2 final branch or commit
- feature/material-phase3-catalog-foundation
- c536f2287c3887103e6a1a2929661ac70a850ca6
- 5eb3984

If the Phase 2 final branch or commit cannot be identified, STOP and report the blocker. Do not guess.

========================================================
PHASE 1 — CREATE OR RECOVER INTEGRATION BRANCH
========================================================

Use:

integration/material-intelligence-v2

If this branch already exists remotely:

- fetch it
- inspect its history
- confirm which Phase 2 and Phase 3 commits it contains
- do not overwrite it

If it does not exist:

- create it from the approved Phase 2 final commit
- merge feature/material-phase3-catalog-foundation
- do not squash unless explicitly required
- do not force-push

Record:

- base commit
- Phase 2 commit
- Phase 3 commit
- merge commit
- conflicts encountered
- conflict resolutions

All conflict resolution must occur only on the integration branch.

========================================================
PHASE 2 — VERIFY ARCHITECTURAL BOUNDARIES
========================================================

Confirm the final branch contains these isolated domains:

- material-library
- material-intelligence
- material-catalog-integration

Verify Phase 3 changes remain additive:

- 20 files added
- no existing Material Library file replaced
- no Material Intelligence file replaced
- no shared route file modified merely to activate Phase 3
- no production startup registration
- no database migration
- no canonical material write

Search for:

- MATERIAL_CATALOG_INTEGRATION_ENABLED
- registerProvider(
- mockOfficialCatalogProvider
- import-preview
- material-catalog
- setInterval
- cron
- startup initialization

Required result:

- feature flag defaults false
- Phase 3 route is unmounted
- provider registry is empty at startup
- mock provider is test-only
- no scheduled process exists
- no external catalog fetch occurs
- no database write occurs

========================================================
PHASE 3 — PHASE 1 MATERIAL LIBRARY REGRESSION
========================================================

Verify the canonical Material Library remains unchanged.

Required checks:

- at least 500 active materials
- unique material codes
- expected category count
- material detail endpoint
- category filtering
- brand filtering
- finish filtering
- color filtering
- price-tier filtering
- draft persistence
- approval snapshot
- prompt-generation compatibility
- image-generation compatibility

Run all available Material Library tests.

Do not rely on old test totals. Report the exact current test files and totals.

If a previously reported test file has been renamed or removed, identify the replacement test coverage.

========================================================
PHASE 4 — PHASE 2 MATERIAL INTELLIGENCE UAT
========================================================

Verify these routes:

GET /material-library/search
GET /material-library/suggestions
GET /material-library/:id/similar
GET /material-library/intelligence/analytics

Required API cases:

1. search q=marble → 200
2. search q=marmer putih → 200
3. search q=kayu jati → 200
4. search q=matte black → 200
5. search q=travertine beige → 200
6. fuzzy search q=marbel → relevant results
7. invalid mode → 400
8. known material ID similar → 200
9. unknown material ID similar → 404
10. anonymous analytics → 401 or 403
11. authorized analytics → 200

Explicit request filters must be hard filters:

- category
- brand
- finish
- color
- priceTier

Required proof examples:

q=marble&priceTier=Budget

Every returned material must have priceTier=Budget.

Repeat equivalent validation for:

- category
- brand
- finish
- color

Query words may influence ranking, but explicit filter parameters must restrict the result set.

========================================================
PHASE 5 — SEARCH QUALITY UAT
========================================================

Evaluate search results for:

- exact matches
- partial keyword matches
- Indonesian aliases
- English aliases
- fuzzy typo tolerance
- hybrid ranking
- category relevance
- brand relevance
- finish relevance
- price-tier relevance
- deterministic ordering

For every query, report:

- query
- mode
- result count
- top five material codes
- top five material names
- score or ranking explanation where available
- cache hit or miss
- latency

Confirm repeated identical requests return deterministic ordering.

========================================================
PHASE 6 — CACHE AND INVALIDATION UAT
========================================================

Verify version signature behavior:

count:latestUpdatedAt

Test invalidation when:

- a material is inserted
- a material is updated
- a material is activated
- a material is deactivated
- a material is deleted, where deletion is supported

Confirm stale data is invalidated for:

- search
- suggestions
- similar materials

Verify:

- canonical Supabase catalog remains the source of truth
- in-memory snapshot reloads after version changes
- warm queries use cache
- unrelated requests do not incorrectly share cache entries
- tenant or scope data is not leaked through cache keys

Restore any test data after verification.

Do not leave test records in production data.

========================================================
PHASE 7 — PERFORMANCE UAT
========================================================

After a fresh server restart, measure:

- initial catalog load
- first uncached search
- subsequent unique uncached search
- warm cached search
- suggestions request
- similar-material request

Use these queries:

- marble
- marmer putih
- kayu jati
- matte black
- travertine beige
- marbel

For each, report:

- HTTP status
- result count
- search mode
- cold latency
- warm latency
- cache status

Do not hide the initial Supabase catalog-loading cost.

Compare the results against prior measurements only as a reference.

========================================================
PHASE 8 — FRONTEND ENTERPRISE UAT
========================================================

Test MaterialSelectorDialog and InteriorDesignEditor.

Required workflow:

1. Open an Interior Design project.
2. Open MaterialSelectorDialog.
3. Search “marmer putih”.
4. Confirm relevant marble results.
5. Search typo “marbel”.
6. Confirm fuzzy results.
7. Select a category filter.
8. Confirm all results belong to that category.
9. Select a brand filter.
10. Confirm all results belong to that brand.
11. Select a finish filter.
12. Confirm all results use that finish.
13. Select a color filter.
14. Confirm all results match that color.
15. Select priceTier=Budget.
16. Confirm no non-Budget item appears.
17. Open similar materials.
18. Select an alternative material.
19. Confirm a canonical material object is returned.
20. Save the design draft.
21. Reload the page.
22. Confirm selected materials persist.
23. Approve the design.
24. Confirm the approval snapshot is immutable.
25. Trigger image-generation preparation.
26. Confirm material prompt compatibility remains unchanged.

Also verify:

- loading states
- empty states
- error states
- keyboard navigation
- mobile layout
- desktop layout
- no duplicate API calls
- no browser console errors
- no React warnings

Capture screenshots for key UAT steps.

========================================================
PHASE 9 — PHASE 3 FOUNDATION CONTRACT UAT
========================================================

Run all Phase 3 tests from:

artifacts/api-server/src/domains/material-catalog-integration/tests/

Verify the following contracts:

Provider:

- stable providerId
- displayName
- sourceType
- getCapabilities
- validateConfig
- fetchCatalog
- AbortSignal support

Registry:

- registration
- duplicate rejection
- lookup
- deterministic listing
- capability filtering
- enable and disable
- empty startup state

DTO and schemas:

- ExternalCatalogItem
- CatalogFetchContext
- ExternalCatalogResult
- ImportOptions
- ImportPreviewResult
- ImportReport
- MediaReference
- CatalogProviderCapabilities
- CatalogProviderValidationResult

Import safety:

- dryRun literal true
- dryRun false rejected
- maximum 500 records
- invalid items isolated
- deterministic normalization
- structured warnings and errors
- no database writes
- no canonical catalog mutation

Duplicate detection:

- providerId + externalId
- brand + productCode
- brand + productName
- source URL
- exact_duplicate
- possible_duplicate
- conflicting_identity
- invalid
- new

Media safety:

- HTTPS accepted
- HTTP rejected
- FTP rejected
- file scheme rejected
- data scheme rejected
- path traversal rejected
- absolute local paths rejected or unresolved
- provider asset IDs supported

Security:

- secret redaction
- token redaction
- password redaction
- credential redaction
- API-key redaction
- typed errors
- no credential logging
- no raw stack exposure through public output

Document honestly that the 10 MB payload limit is only a declared contract if runtime enforcement has not yet been implemented.

Do not claim it is enforced unless a real enforcement test passes.

========================================================
PHASE 10 — AUTHORIZATION AND SECURITY UAT
========================================================

Verify:

- analytics requires authorized admin/platform access
- no Phase 3 endpoint is exposed
- existing material detail routes retain their intended authorization
- invalid IDs do not expose stack traces
- invalid search modes return safe validation errors
- provider configuration secrets are redacted
- logs do not contain tokens or credentials
- no request can activate Phase 3 by supplying a body parameter
- feature activation depends only on the approved server-side flag

Check whether the analytics counters are bounded.

If counters can grow without limit, report it as a blocker or technical debt.

========================================================
PHASE 11 — TESTS, TYPECHECK, AND BUILDS
========================================================

Run:

- Material Library tests
- Material Intelligence tests
- Phase 3 Foundation tests
- MaterialSelectorDialog tests
- InteriorDesignEditor tests
- AI platform regression tests
- customer portal regression tests
- API production build
- AI platform production build
- customer portal production build
- targeted typecheck for all changed files

Before typecheck, build required workspace libraries such as lib/db where applicable.

Separate results into:

- passing tests
- verified pre-existing failures
- newly introduced failures
- environment-related blockers

Do not change tests merely to obtain green results.

Zero new failures are allowed.

========================================================
PHASE 12 — GIT HYGIENE
========================================================

Report:

- git status --short
- tracked modifications
- untracked files
- ignored files
- commit history
- merge-base
- diff summary against the approved base
- unrelated files accidentally included

Do not commit:

- task prompt TXT files
- attached_assets uploads
- screenshots unless required by project policy
- local environment files
- secrets
- modified .replit files unrelated to integration

The integration branch must be clean before publishing.

========================================================
PHASE 13 — PUBLISH INTEGRATION BRANCH
========================================================

Only if all required checks pass:

Push:

integration/material-intelligence-v2

to:

origin

Do not force-push.
Do not merge to main.

Verify with:

git ls-remote --heads origin integration/material-intelligence-v2

Then perform a temporary fresh checkout and confirm:

- branch fetches successfully
- Phase 1 exists
- Phase 2 exists
- Phase 3 Foundation exists
- Phase 3 flag defaults false
- Phase 3 route remains unmounted
- tests can be discovered
- build can start successfully

========================================================
PHASE 14 — FINAL ENTERPRISE UAT REPORT
========================================================

Create:

docs/material-intelligence-v2-enterprise-uat-report.md

The report must include:

1. repository
2. integration branch
3. base commit
4. Phase 2 commit
5. Phase 3 remote HEAD
6. integration merge commit
7. conflict resolutions
8. changed files
9. Phase 1 regression result
10. Phase 2 API result
11. hard-filter result
12. alias and fuzzy-search result
13. analytics authorization result
14. cache invalidation result
15. performance measurements
16. frontend workflow result
17. persistence result
18. approval snapshot result
19. image-generation compatibility result
20. Phase 3 contract result
21. feature-flag status
22. route-mount status
23. provider-registration status
24. security result
25. test totals
26. typecheck result
27. build results
28. verified pre-existing failures
29. new failures
30. screenshots or evidence paths
31. git status
32. remote push result
33. fresh-checkout verification
34. remaining blockers
35. release recommendation

Use one final verdict only:

A. ENTERPRISE UAT PASSED — READY FOR FINAL MAIN MERGE REVIEW

B. ENTERPRISE UAT PASSED WITH NON-BLOCKING TECHNICAL DEBT

C. ENTERPRISE UAT BLOCKED

D. ENTERPRISE UAT FAILED

Do not merge to main.
Do not enable Phase 3.
Do not begin Phase 4.
STOP.