CONTINUE WP-09 FROM CURRENT WORKING TREE — DO NOT RESTART OR REDISCOVER

Current WP:
WP-09 — Rendering Pipeline

STATE SUDAH TERKUNCI:

* approved snapshot = immutable canonical source
* scene spec = deterministic
* Universal Renderer = deterministic/pre-render foundation
* existing Replicate/provider path = photorealistic final render
* existing queue/job/storage/cost tracking reused
* no new table/provider/queue/storage architecture
* 127 legacy TypeScript errors = OUT OF SCOPE

CURRENT PROGRESS:

* WP-09 service foundation sudah ada
* approved snapshot sudah dinormalisasi + di-hash
* render session dibuat sebelum fan-out job
* Universal Renderer sudah dipakai
* photorealistic output memakai provider existing
* shared library artifacts sedang direbuild untuk memisahkan error WP-09 dari baseline

LANJUTKAN LANGSUNG:

1. Rebuild required shared packages/dist terlebih dahulu.
2. Pastikan WP-09 NEW type errors = 0.
3. Finalkan:

   * worker
   * queue adapter
   * `requiredCapability = universal_render` pada canonical capability field
   * tenant/project scoped routes
   * API contracts
   * idempotency
   * retry/job lifecycle
   * provider timeout/failure handling
   * storage/assets
   * ai_cost_records
4. Finalkan InteriorDesignEditor Render panel:

   * Start Render
   * status/progress
   * variants
   * final images
   * failure/retry
   * approved source read-only
5. Jalankan WP-09 targeted tests:

   * approved snapshot
   * missing/unapproved snapshot
   * deterministic scene
   * duplicate/idempotent request
   * tenant isolation/IDOR
   * unauthorized/invalid UUID
   * queue capability
   * job status transitions
   * retry
   * provider failure/timeout
   * storage/assets
   * cost tracking
   * render variants
   * source immutability
   * resource caps
   * UI loading/success/error/retry
6. Jalankan full validation:

   * Interior Design regression
   * API regression
   * AI Platform regression
   * shared typecheck
   * WP-09 typecheck delta
   * API build
   * AI Platform typecheck/build
   * runtime smoke

REQUIRED:

* WP-09 new errors = 0
* P0 = 0
* P1 = 0
* legacy 127 errors tetap OUT OF SCOPE

7. RC audit WP-09.
8. Fix hanya blocker WP-09.
9. Git hygiene harus ZERO:

   * attached_assets
   * prompt/session
   * screenshot
   * .replit
   * memory
   * secret
   * unrelated technical debt
10. Commit cleanly.
11. Push branch:
    `feature/wp09-rendering-pipeline`
12. Create PR to `main`.
13. Audit REMOTE PR.
14. Jika clean + mergeable + validation green + P0/P1 zero:
    MERGE via GitHub PR workflow.
15. Sync `main`.
16. Jalankan post-merge validation.
17. Jika PASS:
    `WP-09 COMPLETE — MERGED AND VERIFIED`
18. Setelah itu hanya discover next canonical Interior Design/Layout WP dan buat plan/branch.
    JANGAN implement next WP.

JANGAN:

* kembali ke main untuk memulai ulang implementasi
* ulang discovery
* ulang owner decisions
* membuat arsitektur paralel
* membersihkan legacy errors
* berhenti hanya untuk progress report

Teruskan otomatis sampai final gate atau blocker eksternal nyata.

FINAL REPORT ONLY:

* final WP-09 SHA
* files changed
* worker/queue/service/routes/UI
* requiredCapability
* provider/storage/cost reuse
* targeted tests
* regressions
* typecheck delta
* P0/P1
* PR
* merge SHA
* final main SHA
* post-merge status
* next WP title/scope

VERDICT:
A. WP-09 MERGED AND VERIFIED — NEXT WP READY
B. WP-09 IMPLEMENTED AND VERIFIED — READY FOR PR
C. WP-09 VALIDATION FAILED
D. WP-09 ARCHITECTURE BLOCKED
E. GITHUB/PROVIDER EXTERNAL BLOCKER
